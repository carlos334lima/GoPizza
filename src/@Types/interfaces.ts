export type TypeProps = "primary" | "secondary";

export enum TypeShowMessage {
  aviso = "warning",
  erro = "danger",
  sucesso = "success",
  default = "default",
  info = "info",
  none = "none",
}
